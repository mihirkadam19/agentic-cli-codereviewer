[project]
name = "codechk"
version = "0.1.2"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "typer>=0.12.0",
    "pydantic>=2.0",
    "langgraph>=1.2.0,<2.0.0",
]

[project.scripts]
codechk = "app.cli:cliApp"

[tool.setuptools.packages.find]
include = ["app*"]
